/*
	Generates N choose M modulo MOD.
*/

	static long c[][] = new int[max][max];
	static
	{
		for( int i = 1; i < max; i++ ) c[i][0] = 1;
		for( int i = 1; i < max; i++ ) for( int j = 1; j < max; j++ ) c[i][j] = ( c[i-1][j] + c[i-1][j-1] ) % MOD;
	}
